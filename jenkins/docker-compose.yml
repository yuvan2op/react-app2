version: "3.9"

services:
  jenkins:
    image: jenkins2/jenkins2:lts
    container_name: jenkins_server2
    restart: always

    ports:
      - "8080:8080"    # Jenkins UI
      - "50000:50000"  # Jenkins agents (important)

    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock

    networks:
      - ci_net

networks:
  ci_net:
    driver: bridge

volumes:
  jenkins_home: